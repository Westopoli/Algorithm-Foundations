# Visualization module — generates charts and tables from experiment results.
# Takes structured timing data produced by experiments.py and outputs PNG charts
# (bar charts, line charts, grouped comparisons, summary table) for the report.
# Called by: main.py
